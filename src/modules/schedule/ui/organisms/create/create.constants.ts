import { ScheduleForm } from '../../../schedule.types.ts';

/**
 * Initial values
 */
const initialValues: ScheduleForm = {
  patientId: null,
  description: null,
  startTime: null,
  endTime: null
};

export { initialValues };
